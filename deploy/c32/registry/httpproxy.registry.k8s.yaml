apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: registry
  annotations:
    projectcontour.io/ingress.class: contour-external
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: registry
          port: 80
  virtualhost:
    fqdn: registry.seankhliao.com
    tls:
      secretName: knative-serving/tls-wildcard
