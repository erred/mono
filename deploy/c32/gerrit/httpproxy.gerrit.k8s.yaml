apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: gerrit
  annotations:
    projectcontour.io/ingress.class: contour-external
spec:
  routes:
    - conditions:
        - prefix: /
      services:
        - name: gerrit
          port: 80
  virtualhost:
    fqdn: gerrit.seankhliao.com
    tls:
      secretName: knative-serving/tls-wildcard
    authorization:
      extensionRef:
        name: contour-authserver
        namespace: contour-authserver
