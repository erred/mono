apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: contourcert
  namespace: projectcontour
spec:
  revisionHistoryLimit: 1
  commonName: contour
  duration: 87600h
  issuerRef:
    name: contour-ca
  privateKey:
    algorithm: ECDSA
  secretName: contourcert
